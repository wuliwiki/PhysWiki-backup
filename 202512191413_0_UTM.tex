% UTM 虚拟机（macOS）笔记
% license Usr
% type Note

\begin{itemize}
\item 参考\href{https://www.machunjie.com/linux/1570.html}{这里}， 没有提到的就按照默认来选即可
\item 关键是添加 serial 端口的 terminal，否则安装无法使用命令行。
\item 换源 \verb`sudo sed -i 's/us.ports.ubuntu.com/mirrors.aliyun.com/' /etc/apt/sources.list`
\item \verb`sudo apt update` 成功
\end{itemize}
