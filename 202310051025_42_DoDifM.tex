% 可微映射的导数
% keys 导数|可微映射
% license Xiao
% type Tutor

\pentry{常微分方程的几何图像\upref{GofODE}}
可微映射（\autoref{def_GofODE_2}~\upref{GofODE}）$f:U\rightarrow V$ 将 $\mathbb R^n$ 空间的区域 $U$ 映射到 $\mathbb R^m$ 空间区域 $V$，于是就将 $U$ 上的曲线（\autoref{sub_GofODE_1}~\upref{GofODE}）映射到 $V$ 上的曲线。